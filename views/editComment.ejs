<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Project</title>
  <script src="https://cdn.tailwindcss.com"></script>

    <link rel="icon" href="favicon.ico" />
</head>
<body class="bg-gradient-to-br from-zinc-900 via-blue-950 to-purple-900 text-white min-h-screen flex items-center justify-center">

  <div class="bg-zinc-800/80 rounded-2xl shadow-2xl border border-blue-700 max-w-lg w-full p-8 flex flex-col gap-7 backdrop-blur-md">
    <h2 class="text-2xl font-bold text-blue-400 text-center mb-2 drop-shadow">
      ✏️ Edit Your Comment
    </h2>
    <form method="POST" action="/comment/edit/<%= comment._id %>" class="w-full flex flex-col gap-6">
      <div class="relative">
        <textarea 
          name="content"
          rows="5"
          class="peer w-full bg-zinc-900/80 text-zinc-200 border border-blue-700 rounded-xl px-4 pt-7 pb-3 transition focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none shadow-inner font-medium"
          required
        ><%= comment.content %></textarea>
        <label class="absolute left-4 top-2 text-blue-400 text-sm font-semibold pointer-events-none transition-all peer-focus:text-blue-500 peer-focus:top-2 peer-valid:top-2">
          Your Comment
        </label>
      </div>
      <div class="flex justify-between items-center">
        <a href="javascript:history.back()" class="text-zinc-400 hover:text-pink-400 underline transition text-sm">Cancel</a>
        <button type="submit"
          class="bg-gradient-to-r from-blue-500 via-purple-600 to-pink-500 text-white font-bold px-7 py-2 rounded-xl shadow-lg hover:scale-105 active:scale-95 transition flex items-center gap-2"
        >
          <span class="material-symbols-outlined text-lg">done</span>
          Update Comment
        </button>
      </div>
    </form>
  </div>

  <!-- Google Material Icons (optional: for checkmark icon) -->
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />

</body>
</body>
</html>
